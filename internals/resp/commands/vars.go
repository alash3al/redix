package commands

// errors as strings
const (
	incorrectArgsCount = "incorrect argument length"
)

// linux style prefixes for separating data
var (
	usrPrefix = []byte("/usr/")
	sysPrefix = []byte("/sys/")
)
